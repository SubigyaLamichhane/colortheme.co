export function hubMetaTitle(name: string, count = 500) {
  return `[${count}+] ${name} with Hex Codes`;
}

export function hubMetaDescription(name: string) {
  return `Discover curated ${name.toLowerCase()} with names, HEX, RGB, HSL, CMYK and more for design inspiration.`;
}
